<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Submissions" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">#1B263B</item>

    </style>

    <style name="Theme.Submissions" parent="Base.Theme.Submissions" />

    <style name="Theme.App.SplashScreen" parent="Theme.Submissions">
        <item name="android:windowBackground">@color/blue</item>
        <item name="windowSplashScreenBackground">@color/blue</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/pompom</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Submissions</item>
    </style>

</resources>